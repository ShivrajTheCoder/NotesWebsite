<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Notes</title>
    <link rel="stylesheet" href="./index.css">
</head>
<body>
     <h1>Here are your all notes</h1>
     <% if(notes.length===0){ %>
     <h2>You will have notes here</h2>
     <% }else{ %>
        <div>
        <%for(let note of notes){%>
            <div>
                <h2><%= note.title %></h2>
                <p><%= note.content %></p>
            </div>
            <form action="/mynotes/<%=note._id%>?_method=DELETE" method="POST">
            <button>Delete</button>
            </form>
            <form action="/mynotes/<%=note._id%>" method="GET">
            <button>Edit</button>
            </form>
       <% } %>
       
    </div>
        <% } %> 
         

     <a href="/mynotes/add">Add a note</a>
</body>
</html>